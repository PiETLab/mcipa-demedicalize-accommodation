{
  "name": "mcipa-demedicalize-accommodation",
  "version": "0.1.0",
  "description": "MCIPA ActionForge pilot — demedicalizing accommodation: public prototype",
  "private": true,
  "scripts": {
    "sync-quartz-config": "ln -sf node_modules/@jackyzha0/quartz/quartz quartz && cp quartz.config.ts node_modules/@jackyzha0/quartz/quartz.config.ts && cp quartz.layout.ts node_modules/@jackyzha0/quartz/quartz.layout.ts",
    "postinstall": "npm run sync-quartz-config",
    "prebuild": "npm run sync-quartz-config",
    "build": "quartz build",
    "prepreview": "npm run sync-quartz-config",
    "preview": "quartz build --serve",
    "presync": "npm run sync-quartz-config",
    "sync": "quartz sync",
    "pretest": "npm run sync-quartz-config",
    "test": "node tests/passage-identifiers.test.cjs && node tests/public-issue-format.test.cjs"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "devDependencies": {
    "@jackyzha0/quartz": "github:jackyzha0/quartz#v4.5.2"
  }
}
